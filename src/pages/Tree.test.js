import { render, screen } from "@testing-library/react";
import Tree from "./Tree";
import { expect, test } from "vitest";

test("User sees a clear visualization of the learning path they asked for", () => {
  // render(<App />);
  // const headingElement = screen.getByText(/learn react/i);
  // expect(headingElement).not.toBeInTheDocument();
    throw new Error;
});

test("If the only highlighted node is the root node, the user is invited to take a series of skill checks to determine the edge skill", () => {
    throw new Error;
});

test("There is an easy way for the user to proceed along the learning path in a clear linear manner", () => {
    throw new Error;
});

test("The user's progress is clearly highlighted", () => {
    throw new Error;
});

test("Successful skill checks highlight a skill node", () => {
    throw new Error;
});

test("Highlighted paths and nodes are saved locally", () => {
    throw new Error;
});